# goldwashplants.com — full SEO audit, 13 August 2026

Measured against the live site, the built output, Google Search Console
(screenshot supplied by Bryce) and the SEMrush US database. Every number below
was pulled on 13 Aug 2026 unless dated otherwise.

---

## The short version

The site is technically clean. That is not the problem.

The problem is that **commercial search terms slid 4–8 positions between 15 July
and today**, while informational terms held or gained. SEMrush's traffic estimate
halved (144 → 68 visits/mo). Actual Search Console clicks did **not** collapse —
748 clicks over roughly three months, flat to slightly up. Both things are true,
and the difference between them is the whole story: the site still earns clicks
from long-tail and brand-adjacent queries, but it is losing the terms that
signal someone is ready to buy a wash plant.

Two defects found this session were live and doing damage. Both are fixed.

---

## 1. Four product/location images were 404ing — including the one Chase reported

Chase asked about the missing M100 photo on **7 August** and again on **9 August**.
He was right, and the cause was a one-word path bug.

Four `<img>` tags used a **relative** `../images/…` path on pages that live one
directory deep. `/products/100-ton/` + `../images/` resolves to
`/products/images/m100-machine.jpg`, which returns **404**. The image file itself
was present and served fine at `/images/m100-machine.jpg` (200).

| Page | Image | Status before |
|---|---|---|
| `/products/100-ton/` | `m100-machine.jpg` | 404 |
| `/products/50-ton/` | `m50-machine.jpg` | 404 |
| `/locations/alaska/` | `loading-washplant.jpg` | 404 |
| `/locations/alaska/` | `washplant-action.jpg` | 404 |

`/products/200-ton/` used an absolute `/images/…` path and was never affected —
which is why this went unnoticed.

**Fixed** in `c2a6cd2`. All four now use absolute paths, with descriptive alt text
and `loading="lazy"` to match the 200-ton pattern.

⚠️ **Watch for this pattern.** Any new page one directory deep that uses
`src="../images/…"` will break the same way. The audit script in
`/private/tmp/.../audit.py` checks for it; worth moving into the build.

---

## 2. The site published 145 reviews it does not have

This is the most serious finding and it had been flagged before.

Live structured data declared:

- Homepage `Organization` → `aggregateRating` **4.8 from 127 reviews**
- `/products/50-ton/` `Product` → `aggregateRating` **4.9 from 18 reviews**

The 22 July session recorded that the site has **zero reviews** — that was named
as "the biggest local handicap." The 19 May session had already flagged the
homepage rating: *"verify this is real and backed by collected reviews, or remove
it (fake rating markup is a manual-action risk)."* It was never resolved.

Self-serving `aggregateRating` markup that is not backed by reviews collected and
displayed on the page violates Google's structured data policy. The usual outcome
is not a dramatic penalty — it is Google quietly discounting the site's structured
data, and in worse cases a manual action against rich results. Search Console's
sidebar shows **Review snippets**, **Product snippets** and **Merchant listings**
reports, all of which read this markup.

The same page also carried an `Offer` declaring **price $35,000** and **$0
shipping** — on a page that publishes no price, for a company that ships
worldwide. The other three product pages deliberately carry no `Offer` for
exactly this reason (documented 19 May).

**Fixed** in `c2a6cd2`: both `aggregateRating` blocks and the 50-ton `Offer`
removed. Zero `aggregateRating` remain in the build.

🔴 **Open question for Chase.** The three homepage testimonials (Mike R. /
Fairbanks, James T. / Grass Valley, Kwame A. / Accra) are still on the page. They
are plausible and may well be real, but nobody here can confirm them. If they are
real, get those customers to leave actual Google reviews — that converts an
unverifiable claim into a ranking asset. If they are not, they should come down.

---

## 3. Keyword cannibalization — diagnosed 22 July, never fixed, now worse

The 22 July session called this "the cheapest available gain, no new content
needed." Nothing was done, and the money terms have slid.

Same-source SEMrush comparison, 15 July vs 13 August:

| Keyword | Vol/mo | 15 Jul | 13 Aug | Change |
|---|---|---|---|---|
| `gold wash plant` | 390 | **7** | **15** | 🔴 −8 |
| `gold wash plants` | 70 | **5** | **12** | 🔴 −7 |
| `gold wash plant for sale` | 210 | **4** | **8** | 🔴 −4 |
| `gold in clay deposits` | 110 | 6 | dropped out of top 30 | 🔴 |
| `gold wash plants for sale` | 110 | 3 | 3 | flat |
| `wash plant for sale` | 110 | 6 | 6 | flat |
| `small scale gold wash plants for sale` | 90 | 4 | 4 | flat |
| `flour gold` | 480 | — | **5** | 🟢 new, biggest volume on site |

The pattern is unambiguous: **every term with buying intent went down; the
informational blog terms held.**

Eight terms are now split across multiple URLs:

| Keyword | Vol | URLs | Positions |
|---|---|---|---|
| `easy wash plant` | 210 | **6** | 5 / 27 / 36 / 42 / 48 / 55 |
| `gold wash plant for sale` | 210 | **5** | 8 / 29 / 43 / 52 / 63 |
| `wash plants` | 90 | **5** | 28 / 45 / 67 / 79 / 92 |
| `gold wash plant` | 390 | 4 | 15 / 30 / 44 / 57 |
| `gold wash plants` | 70 | 4 | 12 / 31 / 44 / 55 |
| `gold mining wash plant` | 70 | 4 | 13 / 30 / 45 / 54 |
| `wash plant for gold` | 70 | 4 | 10 / 30 / 40 / 52 |
| `gold wash machine` | 40 | 3 | 27 / 52 / 54 |

`wash plants` sits at **exactly** the same five positions as on 22 July —
28/45/67/79/92. Three weeks, zero movement.

Note the titles are already differentiated (`/products/` is "Models & Specs",
`/how-it-works/` is "How Gold Wash Plants Work") — that was the 22 July fix and it
was the right one. The remaining split is driven by body copy and internal
linking, not by title tags. **This is the next piece of work and it is the highest
expected return of anything on this list.**

---

## 4. The money pages were the thinnest pages on the site

Before this session:

| Page | Words |
|---|---|
| `/products/300-ton/` | 265 |
| `/products/100-ton/` | 292 |
| `/products/200-ton/` | 295 |
| `/products/50-ton/` | 309 |

These are the pages that need to rank for `gold wash plant for sale`, and they
ranked 29th–63rd for it. The homepage, at 10× the length, ranked 8th.

**Fixed** in `c2a6cd2`: each product page gained a model-specific FAQ section plus
`FAQPage` schema, answering the questions people actually search — cost, water
requirement, power, feed size, fine-gold recovery, international shipping. Every
answer links into the existing blog cluster, which also fixes the "blog posts are
stranded" problem noted on 15 May.

Product pages now run **629–655 words**. All facts are taken from each page's own
spec table; nothing was invented.

---

## 5. The 293 "not indexed" pages are mostly legacy, and mostly benign

Search Console reports **46 indexed / 293 not indexed**. That looks alarming and
mostly is not.

The site publishes **57 pages** (56 in the sitemap, `/hq/` correctly excluded and
noindexed). So ~283 of those 293 URLs are **not current site pages**. They are
almost certainly legacy URLs from the old **goldwatchproject.com** WordPress site —
tag archives, category archives, attachment pages, paginated archives. That kind of
bloat generates hundreds of URLs, and Google reporting them as "Page with redirect"
is expected, not a fault.

Sitemap health was verified directly: **all 56 URLs return 200 with no redirect
hops.** Location and blog pages were tested for near-duplication and none exceeded
70% similarity, so duplicate content is not the cause either.

**The real number to care about is 46 indexed out of 56 indexable** — roughly ten
real pages are not in the index. Identifying which ten requires the Search Console
Pages report (filter: *Crawled — currently not indexed* and *Discovered — currently
not indexed*). That is a five-minute job for whoever has GSC open.

---

## 6. 🔴 The migration redirects were built in May and never shipped

This is the most valuable unshipped work on the account.

`goldwatchproject.com` currently **301s every URL to the goldwashplants.com
homepage**. Verified live:

```
https://goldwatchproject.com/blog                     -> https://goldwashplants.com/
https://goldwatchproject.com/wash-plant-pricing       -> https://goldwashplants.com/
https://goldwatchproject.com/gold-wash-plant-info     -> https://goldwashplants.com/
… all 16 mapped URLs, same result
```

A blanket redirect to the homepage is treated by Google as a soft 404. It passes
close to **no link equity**. With 132 referring domains — many of which point at
the old domain — this is throwing away the site's single scarcest asset.

A page-level redirect map fixing exactly this was written on **26 May** and
committed to branch `seo/migration-redirect-map-2026-05`. **It was never merged
and never applied.** The file is at
`~/Code/goldwashplants-site/docs/goldwatchproject-redirects.csv` — 16 rows, in
Cloudflare Bulk Redirects format.

All 16 currently miss their intended target. Applying it is a **Cloudflare change
on the goldwatchproject.com zone**, not a code deploy — so it needs someone with
Cloudflare access, not a merge.

**Do this before writing any new content.** New content builds authority slowly;
recovering existing link equity is immediate.

---

## 7. Smaller items

**🔴 The 300-ton page contradicts itself about sluice length, live right now.**
The spec table and `Product` schema say **50 feet**. The meta description, the
hero paragraph and the body copy say **60 ft**. Four places, two answers. This is
a product fact — *do not guess it.* One question for Chase, then fix everywhere.

**No JSON-LD on six pages**: `/blog/`, `/compare/`, `/contact/`, `/examples/`,
`/privacy/`, `/terms/`. The last two do not need it. `/contact/` and `/blog/` are
worth adding.

**`/contact/` is 86 words** — the thinnest page on the site, and it is a
conversion page.

**Video: 4 videos known to Google, 0 indexed.** `video-sitemap.xml` is served
(200) and was last touched 15 May. Worth a look — video results are cheap traffic
in this category.

**Two stale checkouts of this repo exist.** `~/Documents/clients/goldwashplants`
is canonical (on `main`, current with origin). `~/Code/goldwashplants-site` is
parked on the unmerged May branch and is **three months behind** — do not work in
it. It is the only place the redirect CSV currently lives.

**Spam-filter false positives are still open** (Phase 1 from 22 July, never
shipped): `LLC` company names rejected by the consonant-run rule, and `y` treated
as a consonant so `dyby6178@gmail.com` reads as vowel-less. Both hit real leads.

---

## What to do next, in order

1. **Apply the goldwatchproject.com redirect map in Cloudflare.** Highest return,
   costs nothing, recovers equity that already exists. *Needs Cloudflare access.*
2. **Ask Chase the 300-ton sluice question** and fix the contradiction.
3. **De-cannibalize the eight split terms.** Pick one owner page per term, rewrite
   competing body copy, point internal links at the owner with exact-match anchors.
4. **Pull the Search Console Pages report** and identify the ~10 real pages not
   indexed.
5. **Reviews.** Zero reviews is now the honest position, and it is the biggest
   remaining local handicap. Getting ten real Google reviews would do more than
   another ten blog posts.
6. Ship the Phase 1 spam-filter fixes.
7. Add schema to `/contact/` and `/blog/`; expand `/contact/`.

---

## Verified this session

- 57 pages build clean; **120 JSON-LD blocks, 0 invalid**
- 0 `aggregateRating` remaining in the build
- All 56 sitemap URLs return 200, no redirect hops
- 0 images missing alt text; 0 relative `../images/` references remain
- 0 duplicate titles; 0 duplicate meta descriptions
- Canonicals present and self-referential on every page except `/hq/` (noindexed
  by design)
- Location, product and blog pages: no pair above 70% body-text similarity
